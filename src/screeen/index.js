import { SearchPanel } from "./search-panel"
import { ProjectList } from "./project-list"

export const ProjectScreen = () => {
    return (
        <div>
            <SearchPanel />
            <ProjectList />
        </div>
    )
}